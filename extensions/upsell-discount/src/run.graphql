query RunInput {
  cart {
    lines {
      id
      quantity
      cost {
        totalAmount{
          amount
        }
      }
      discount: attribute(key:"discountAdd"){
        value
      }
    }
  }
}
